#include <stdio.h>
int main()
{
	int n,sum,i;
	printf("Enter The N Value:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		sum=sum+i*i*i;
		if(i<n)
		{
			printf("%d^3+",i);
		}
		else
		{
			printf("%d^3=%d",i,sum);
		}
	}
}
