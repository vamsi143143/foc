#include <stdio.h>
int main()
{
	int n,sum=0,i;
	printf("Enter The N Value:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		sum=sum+i*i;
		if(i<n)
		{
			printf("%d^2+",i);
		}
		else
		{
			printf("%d^2=%d",i,sum);
		}
	}
}
