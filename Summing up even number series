#include <stdio.h>
int main()
{
	int n,i;
	printf("Enter The N Value:");
	scanf("%d",&n);
	if(n%2==1)
	{
		n=n-1;
	}
	for(i=1;i<=n;i++)
	{
		if(i%2==0)
		{
			if(i<n)
			{
				printf("%d,",i);
			}
			else
			{
				printf("%d",i);
			}
		}
	}
}
